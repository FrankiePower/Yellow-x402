# ── ClearNode ──────────────────────────────────────────────
CLEARNET_URL=wss://clearnet-sandbox.yellow.com/ws

# ── Wallets (different keys for buyer and service) ─────────
# Both accounts need to exist; sandbox ytest.usd is auto-funded via faucet.
BUYER_PRIVATE_KEY=0x...
SERVICE_PRIVATE_KEY=0x...

# ── Service ─────────────────────────────────────────────────
SERVICE_PORT=4000
PRICE=1000000          # amount in ytest.usd base units

# ── Buyer ───────────────────────────────────────────────────
SERVICE_URL=http://localhost:4000

# ── Channel / on-chain settlement ────────────────────────────
CHAIN_ID=11155111                                            # Sepolia
TOKEN_ADDRESS=0xDB9F293e3898c9E5536A3be1b0C56c89d2b32DEb     # ytest.usd on ClearNode sandbox
# Buyer wallet needs Sepolia ETH for gas if on-chain settlement is attempted.

# ── Debug ───────────────────────────────────────────────────
# Set to 1 to log all raw WebSocket messages from ClearNode
# DEBUG=1
